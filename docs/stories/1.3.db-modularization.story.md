# Story 1.3: Modularização do `db.ts`

---

## Status

**Approved** ✅ — Validado por Pax (PO) em 2026-03-06. Score: 9/10, 0 issues críticos.

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["npm test", "npm run build"]
```

---

## Story

**As a** desenvolvedor,
**I want** o arquivo `db.ts` (71KB, 2.114 linhas, 89 métodos) separado em stores por domínio,
**so that** eu consiga isolar responsabilidades, facilitar testes unitários e manter o código com segurança.

---

## Acceptance Criteria

1. `db.ts` é separado em stores em `src/storage/stores/`: `messages-store.ts`, `scripts-store.ts`, `triggers-store.ts`, `tags-store.ts`, `folders-store.ts`, `settings-store.ts`, `signatures-store.ts`, `schedules-store.ts`, `notes-store.ts`, `kanban-columns-store.ts`, `kanban-leads-store.ts`
2. Lead Normalization/Merge (~430 linhas, métodos L211–L649) extraído para `src/storage/lead-utils.ts`
3. Arquivo principal `db.ts` mantém apenas: interface `PrinChatDB` (schema), classe `DatabaseService` com `init()`, `upgrade()`, `initializeDefaults()` e migrations
4. Todas as queries existentes continuam funcionando — zero regressão
5. Migração de schema é backward compatible — nenhuma alteração no IndexedDB schema
6. `syncService` acessível por stores que dependem dele (kanban-leads-store usa `syncService.syncLead()`)
7. Testes existentes (`src/storage/__tests__/db-utils.test.ts`) continuam passando
8. Build de produção (`npm run build`) funciona sem erros

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] **Task 1: Criar estrutura de diretórios** (AC: 1)
  - [x] Criar `src/storage/stores/` directory
  - [x] Criar `src/storage/lead-utils.ts`

- [x] **Task 2: Extrair Lead Normalization/Merge para `lead-utils.ts`** (AC: 2, 6)
  - [x] 15+ funções extraídas (465 linhas): normalizeScope, matchesScope, normalizeLeadIdentity, normalizeLeadChatId, hasRenderablePhoto, isLikelyPlaceholderPhotoUrl, hasValidLeadName, getLeadQualityScore, buildCanonicalChatIdForLead, normalizeAndMergeKanbanColumns, normalizeAndMergeLeads, resolveLeadStorageKey, resolveScopedLeadAliases
  - [x] Convertidas de private methods para exported functions recebendo db handle
  - [x] Imports de syncService, buildScopedLeadId, normalizeChatIdentity, normalizeInstanceId mantidos

- [x] **Task 3: Extrair Messages Store** (AC: 1, 4)
  - [x] `src/storage/stores/messages-store.ts` (291 linhas) — saveMessage, getMessage, getAllMessages, deleteMessage, getMessagesByType + handleMediaUpload helper

- [x] **Task 4: Extrair Scripts Store** (AC: 1, 4)
  - [x] `src/storage/stores/scripts-store.ts` (49 linhas) — saveScript, getScript, getAllScripts, deleteScript

- [x] **Task 5: Extrair Triggers Store** (AC: 1, 4)
  - [x] `src/storage/stores/triggers-store.ts` (41 linhas) — saveTrigger, getTrigger, getAllTriggers, getEnabledTriggers, deleteTrigger

- [x] **Task 6: Extrair Tags Store** (AC: 1, 4)
  - [x] `src/storage/stores/tags-store.ts` (56 linhas) — saveTag, getTag, getAllTags, deleteTag (com syncService)

- [x] **Task 7: Extrair Folders Store** (AC: 1, 4)
  - [x] `src/storage/stores/folders-store.ts` (62 linhas) — saveFolder, getFolder, getAllFolders, deleteFolder (callbacks para evitar circular import)

- [x] **Task 8: Extrair Settings Store** (AC: 1, 4)
  - [x] `src/storage/stores/settings-store.ts` (39 linhas) — saveSettings, getSettings

- [x] **Task 9: Extrair Signatures Store** (AC: 1, 4)
  - [x] `src/storage/stores/signatures-store.ts` (101 linhas) — saveSignature, getSignature, getAllSignatures, getActiveSignature, setActiveSignature, deleteSignature

- [x] **Task 10: Extrair Schedules Store** (AC: 1, 4)
  - [x] `src/storage/stores/schedules-store.ts` (138 linhas) — saveSchedule, getSchedule, getAllSchedules, getSchedulesByChatId, getPendingSchedules, getAllPendingSchedules, getPausedSchedules, updateScheduleStatus, deleteSchedule

- [x] **Task 11: Extrair Notes Store** (AC: 1, 4)
  - [x] `src/storage/stores/notes-store.ts` (131 linhas) — createNote, getNotesByChatId, getNote, updateNote, deleteNote, getAllNotes

- [x] **Task 12: Extrair Kanban Columns Store** (AC: 1, 4, 6)
  - [x] `src/storage/stores/kanban-columns-store.ts` (305 linhas) — initializeDefaultKanbanColumns, saveKanbanColumn, getKanbanColumn, getAllKanbanColumns, deleteKanbanColumn, updateColumnOrder, createKanbanColumn, updateKanbanColumn

- [x] **Task 13: Extrair Kanban Leads Store** (AC: 1, 4, 6)
  - [x] `src/storage/stores/kanban-leads-store.ts` (290 linhas) — createLead, saveLead, updateLead, getLead, getAllLeads, getLeadsByColumn, moveLead, deleteLead

- [x] **Task 14: Atualizar `db.ts` como orquestrador** (AC: 3, 5)
  - [x] Interface `PrinChatDB` exportada (schema)
  - [x] `DatabaseService` mantém init(), upgrade(), initializeDefaults()
  - [x] Façade delega todas operações para stores importados
  - [x] Singleton `db` mantém mesma API pública — zero mudanças nos 14 call sites

- [x] **Task 15: Criar barrel export** (AC: 1)
  - [x] `src/storage/stores/index.ts` re-exportando todas as 11 stores

- [x] **Task 16: Validação e testes** (AC: 4, 7, 8)
  - [x] `npm test` — 56/56 testes passam (2 test files, 0 failures)
  - [x] `npm run build` — build sem erros, todos content scripts compilados
  - [x] `db.ts` ficou com 668 linhas (schema + init + façade methods)
  - [x] Nenhuma store excede 500 linhas (maior: kanban-columns-store com 305)

---

## Dev Notes

### Contexto Técnico

**Fonte:** `docs/epic1_tech_review.md` — Seção 2.4 + `docs/prd.md` — Story 1.3

O `db.ts` é o **mais fácil dos 4 arquivos** para decompor (parecer da Architect). Motivos:
- Já está organizado por seções com comentários claros (`// === MESSAGES ===`, etc.)
- **Não tem restrição IIFE** — usa imports/exports ES normais (ao contrário dos content scripts)
- Build é feito via Vite bundling normal

### Mapeamento de Domínios no `db.ts` Atual

| Domínio | Linhas | Métodos | Arquivo Destino |
|---------|:------:|:-------:|-----------------|
| Schema + Init + Migrations | 89–209 | 3 | **db.ts** (mantém) |
| Lead Normalization/Merge | 211–649 | ~15 | **lead-utils.ts** |
| Defaults | 651–665 | 1 | **db.ts** (mantém) |
| Messages CRUD | 669–950 | 6 | **messages-store.ts** |
| Scripts CRUD | 952–985 | 4 | **scripts-store.ts** |
| Triggers CRUD | 987–1011 | 5 | **triggers-store.ts** |
| Tags CRUD | 1013–1052 | 4 | **tags-store.ts** |
| Folders CRUD | 1054–1095 | 4 | **folders-store.ts** |
| Settings | 1097–1124 | 2 | **settings-store.ts** |
| Signatures | 1126–1208 | 6 | **signatures-store.ts** |
| Schedules | 1210–1315 | 8 | **schedules-store.ts** |
| Notes | 1317–1430 | 6 | **notes-store.ts** |
| Kanban Columns | 1430–1750 | ~10 | **kanban-columns-store.ts** |
| Kanban Leads | 1750–2110 | ~20 | **kanban-leads-store.ts** |

[Source: `docs/epic1_tech_review.md#2.4`]

### Padrão de Extração Recomendado

Cada store deve exportar funções puras que recebem o `db` handle:

```typescript
// src/storage/stores/messages-store.ts
import type { IDBPDatabase } from 'idb';
import type { PrinChatDB } from '../db';
import type { Message } from '@/types';

export async function saveMessage(
  db: IDBPDatabase<PrinChatDB>,
  message: Message
): Promise<void> {
  // ... implementação movida de DatabaseService.saveMessage()
}

export async function getMessage(
  db: IDBPDatabase<PrinChatDB>,
  id: string
): Promise<Message | undefined> {
  // ... implementação movida
}
```

O `DatabaseService` na `db.ts` mantém a façade:

```typescript
// src/storage/db.ts (simplificado)
import { saveMessage, getMessage, ... } from './stores/messages-store';

class DatabaseService {
  private db: IDBPDatabase<PrinChatDB> | null = null;

  async saveMessage(message: Message): Promise<void> {
    const db = await this.init();
    return saveMessage(db, message);
  }
}
```

[Source: `docs/epic1_tech_review.md#2.4` — "manter db.ts com schema, init e migrations; extrair stores por domínio"]

### Dependências Críticas

- **`syncService`**: Usado em `kanban-leads-store` para `syncLead()` e `deleteLead()`. Importar diretamente em cada store que precisa.
- **`buildScopedLeadId`, `normalizeChatIdentity`, `normalizeInstanceId`**: De `../utils/instance-scope`. Usado em `lead-utils.ts`.
- **`DEFAULT_KANBAN_COLUMNS`**: De `../types/kanban`. Usado em `kanban-columns-store`.
- **`chrome.storage`**: Usado em vários stores para notificar mudanças. Acessar via `typeof chrome !== 'undefined'` pattern.

[Source: `src/storage/db.ts` — imports L6–L11]

### Notas da Story Anterior (1.4)

- Vitest já está configurado e funcional
- 10 testes existentes em `src/storage/__tests__/db-utils.test.ts` e `src/utils/__tests__/instance-scope.test.ts`
- `.bak` arquivos removidos, `.gitignore` atualizado
- `npm test` e `npm run build` passam com sucesso

[Source: Story 1.4 completion — PR #1 merged]

### Source Tree Relevante

```
src/storage/
├── db.ts                    # ALVO: 2.114 linhas → ~250 linhas (schema + init)
├── stores/                  # NOVO: directory para stores extraídos
│   ├── index.ts             # NOVO: barrel export
│   ├── messages-store.ts    # NOVO
│   ├── scripts-store.ts     # NOVO
│   ├── triggers-store.ts    # NOVO
│   ├── tags-store.ts        # NOVO
│   ├── folders-store.ts     # NOVO
│   ├── settings-store.ts    # NOVO
│   ├── signatures-store.ts  # NOVO
│   ├── schedules-store.ts   # NOVO
│   ├── notes-store.ts       # NOVO
│   ├── kanban-columns-store.ts  # NOVO
│   └── kanban-leads-store.ts    # NOVO
├── lead-utils.ts            # NOVO: ~430 linhas de lead normalization
├── chrome-storage.ts        # Existente — não alterar
└── __tests__/
    └── db-utils.test.ts     # Existente — deve continuar passando
```

### Testing

- **Framework:** Vitest (configurado na Story 1.4)
- **Testes existentes:** `src/storage/__tests__/db-utils.test.ts` — devem continuar passando sem modificação
- **Novos testes recomendados (para Story 1.4 restante):** Testes unitários para cada store extraído
- **Localização:** `src/storage/__tests__/` ou `src/storage/stores/__tests__/`
- **Padrão:** Importar funções diretamente dos módulos fonte (não via `DatabaseService`)
- **Mock:** Mock do `IDBPDatabase` para testes de stores individuais

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 1.0.0 | Story draft criada a partir do PRD v1.1.0 e tech review da Architect | River (SM) |

---

## Dev Agent Record

### Agent Model Used

Antigravity (Google Deepmind)

### Debug Log References

- Tests: 56/56 passed (vitest run) — 179ms
- Build: All content scripts compiled successfully
- db.ts: 2114 → 668 lines (68% reduction)

### Completion Notes List

- All 16 tasks completed — zero regression
- PrinChatDB interface now exported for store modules
- Folders store uses callback pattern to avoid circular imports with messages store
- Kanban stores receive lock Maps as parameters to maintain concurrency behavior
- db.ts façade is 668 lines (not 250 as estimated) because it includes all façade methods with proper typing
- All stores are under 500 lines (largest: kanban-columns-store at 305)

### File List

| File | Action | Lines |
|------|--------|------:|
| `src/storage/db.ts` | MODIFIED | 668 |
| `src/storage/lead-utils.ts` | NEW | 465 |
| `src/storage/stores/index.ts` | NEW | 16 |
| `src/storage/stores/messages-store.ts` | NEW | 291 |
| `src/storage/stores/scripts-store.ts` | NEW | 49 |
| `src/storage/stores/triggers-store.ts` | NEW | 41 |
| `src/storage/stores/tags-store.ts` | NEW | 56 |
| `src/storage/stores/folders-store.ts` | NEW | 62 |
| `src/storage/stores/settings-store.ts` | NEW | 39 |
| `src/storage/stores/signatures-store.ts` | NEW | 101 |
| `src/storage/stores/schedules-store.ts` | NEW | 138 |
| `src/storage/stores/notes-store.ts` | NEW | 131 |
| `src/storage/stores/kanban-columns-store.ts` | NEW | 305 |
| `src/storage/stores/kanban-leads-store.ts` | NEW | 290 |

---

## QA Results

### Review By: Quinn (QA Agent) — 2026-03-06

---

### 🔒 Gate Decision: **PASS** ✅

**Confidence:** High | **Risk Level:** Low

---

### Acceptance Criteria Verification

| AC | Critério | Status | Evidência |
|----|----------|--------|-----------|
| 1 | Stores extraídos para `src/storage/stores/` | ✅ PASS | 11 stores criados: messages, scripts, triggers, tags, folders, settings, signatures, schedules, notes, kanban-columns, kanban-leads |
| 2 | Lead Normalization extraído para `lead-utils.ts` | ✅ PASS | 465 linhas, 15+ funções exportadas (normalizeScope, matchesScope, normalizeLeadIdentity, etc.) |
| 3 | `db.ts` mantém schema + init + façade | ✅ PASS | 668 linhas — PrinChatDB interface, DatabaseService com init/upgrade/initializeDefaults + façade |
| 4 | Zero regressão nas queries | ✅ PASS | `npm test` — 56/56 testes passam; 14 call sites verificados, todos usam `{ db }` singleton sem mudanças |
| 5 | Backward compatible (sem alteração no IndexedDB schema) | ✅ PASS | Schema idêntico ao original, mesmo DB_NAME/DB_VERSION, mesma upgrade function |
| 6 | `syncService` acessível por stores dependentes | ✅ PASS | tags-store, signatures-store, schedules-store, notes-store, kanban-columns-store, kanban-leads-store importam `syncService` diretamente |
| 7 | Testes existentes continuam passando | ✅ PASS | `vitest run` → 56/56 passed (2 test files, 0 failures, 248ms) |
| 8 | Build funciona sem erros | ✅ PASS | `tsc && vite build && build-content.cjs` — sucesso completo |

### Code Quality Analysis

**Padrão de extração (consistência):** ✅ Excelente
- Todas as stores seguem o mesmo padrão: funções puras exportadas que recebem `IDBPDatabase<PrinChatDB>` como primeiro parâmetro
- `chrome.storage` notifications consistentes via `typeof chrome !== 'undefined'` guard
- Documentação JSDoc header em todos os arquivos com referência à Story 1.3

**Gestão de dependências circulares:** ✅ Bem resolvido
- `folders-store.ts` usa callback pattern (`getAllMessagesFn`, `saveMessageFn`) para evitar circular import com messages-store
- `messages-store.ts` usa dynamic import (`await import('../../services/media-service')`) para media service
- `lead-utils.ts` usa `IDBPDatabase<any>` em funções de normalização para evitar circular com `db.ts`

**Concurrency (locks):** ✅ Preservado
- `kanbanInitLocks`, `kanbanColumnNormalizationLocks`, `leadNormalizationLocks` (Maps) permanecem no `DatabaseService` e são passados como parâmetros para stores que os necessitam

**Tamanho dos arquivos:**

| Arquivo | Linhas | Conforme (< 500) |
|---------|-------:|:-:|
| kanban-columns-store.ts | 305 | ✅ |
| messages-store.ts | 291 | ✅ |
| kanban-leads-store.ts | 290 | ✅ |
| lead-utils.ts | 465 | ✅ |
| db.ts (orquestrador) | 668 | ✅ |

### Build Warnings (Advisory — Not Blocking)

> ⚠️ **Vite dynamic import warnings**: `media-service.ts` e `db.ts` são dinamicamente importados em alguns pontos mas também estaticamente em outros. Isso é pré-existente e não foi introduzido pela Story 1.3.

> ⚠️ **Chunk size warning**: `options.js` (510KB) excede 500KB. Pré-existente, não relacionado a esta story.

### Risk Assessment

| Risco | Prob. | Impacto | Mitigação |
|-------|:-----:|:-------:|-----------|
| API pública alterada | Baixo | Alto | Singleton `db` mantém mesma API — zero mudanças nos 14 call sites |
| Circular imports | Baixo | Médio | Callback pattern (folders) e dynamic import (messages) resolvem |
| Lock maps perdidos | Baixo | Alto | Passados como parâmetros explícitos do DatabaseService para stores |
| Schema regression | Nulo | Alto | Mesma upgrade function, mesmo DB_VERSION=8 |

### Concerns (Advisory — Low Priority)

1. **`db.ts` com 668 linhas** — acima da estimativa de ~250, mas justificável pela façade completa com tipagem. A complexidade real está nos stores, não no orchestrator.
2. **Debug console.logs abundantes** em `messages-store.ts` (linhas 188–194) e `notes-store.ts`. Recomenda-se cleanup futuro (debt item).
3. **`getMessagesByType` não busca blobs de tipo 'file'** — pré-existente, não introduzido nesta story.
4. **`deleteSignature` não sincroniza deleção** via `syncService.deleteSignature()` — pré-existente, verificar se intencional.

---

— Quinn, guardião da qualidade 🛡️
